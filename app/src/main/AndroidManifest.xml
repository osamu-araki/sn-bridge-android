<?xml version="1.0" encoding="utf-8"?>
<!-- Version: 1.3.0 | Updated: 2026-05-18 -->
<!-- [2026-03-13] ChallengeActivity 追加（Cloudflare 認証の手動解除画面） -->
<!-- [2026-04-11] SYSTEM_ALERT_WINDOW 追加（タップ不要でチャレンジ画面を直接起動） -->
<!-- [2026-05-18] ChromeBridgeApp 登録（未捕捉例外ハンドラ） -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- [2026-03-13] チャレンジ認証画面をバックグラウンドから表示するため -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- [2026-04-11] バックグラウンド Activity 起動制限の回避（通知タップをスキップ） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".ChromeBridgeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChromeBridge"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ChromeBridge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- [2026-03-13] Cloudflare チャレンジ手動認証画面 -->
        <activity
            android:name=".ChallengeActivity"
            android:exported="false"
            android:theme="@style/Theme.ChromeBridge"
            android:showOnLockScreen="true"
            android:turnScreenOn="true" />

        <service
            android:name=".service.BridgeForegroundService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

    </application>
</manifest>
